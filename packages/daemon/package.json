{
  "name": "@torra/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "build": "tsup src/index.ts src/main.ts --format esm --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@torra/agents": "workspace:*",
    "@torra/core": "workspace:*",
    "@torra/git": "workspace:*",
    "@torra/github": "workspace:*",
    "@torra/sandbox": "workspace:*",
    "@torra/usage": "workspace:*",
    "zod": "^4.4.3"
  }
}
